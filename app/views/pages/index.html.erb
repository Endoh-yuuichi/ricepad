<div class="top-hero-wrapper text-center">
  <div class="container top-hero-container">
    <h1 class="text-white">ＣＯＯＫ－ＲＩＣＥ</h1>
    <h2 class="text-white">登録はこちらへ</h2>
    <div class="row">
      <div class="col-md-offset-1 col-md-3">
        <%= link_to '登録', new_user_path, class: 'btn btn-block btn-white' %>
      </div>
      <div class="col-md-offset-1 col-md-3">
        <%= link_to 'ログイン', login_path, class: 'btn btn-block btn-white' %>
      </div>
      <% if logged_in?  %>　
        <div class="col-md-offset-1 col-md-3">
         <%= link_to 'マイページ', user_path(id: current_user.id), class: 'btn btn-block btn-white' %>
        </div>
      <% end %>
  </div>
</div>
